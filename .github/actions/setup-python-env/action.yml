name: "Setup Python Environment"
description: "Set up pixi environment"

runs:
  using: "composite"
  steps:
    - uses: prefix-dev/setup-pixi@v0.8.0
      with:
        pixi-version: v0.62.2
        cache: true
